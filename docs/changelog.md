# 变更日志（docs/changelog.md）

> 追加式记录：日期 | 变更内容 | 涉及文件 | 关联任务/ADR。每次修改必须在此留痕（见 ai-collab.md §2）。

| 日期 | 变更内容 | 涉及文件 | 关联 |
|---|---|---|---|
| 2026-09-02 | 建立项目文档体系：README、AI 协作说明、需求规格、架构说明、里程碑、任务进度、变更日志、11 条 ADR | `09-knowseq-agent/` 全部文档 | T-001 / M0 |
| 2026-09-02 | M1 需求拆分：新建 m1-requirements.md（REQ-101~111）；新增 ADR-012（极简控制台）/ADR-013（inbox 素材模型）；tasks.md 落细分待办 T-101~112；milestones.md 补充指向 | `docs/m1-requirements.md`、`docs/adr/ADR-012-mini-console.md`、`docs/adr/ADR-013-inbox-model.md`、`docs/tasks.md`、`docs/milestones.md` | T-101~112 / M1 |
| 2026-09-02 | M1 代码落地与验证：搭建 .venv（py -3 实为 Python 3.14）安装依赖；验证 REQ-101/102/103/107/108/109/110（控制台 API 闭环、drop 热文件夹自动入库、readability 正文抽取、内容哈希去重、状态机启停）；修复 requirements.txt 缺 python-multipart 导致 `/api/import/file` 启动失败；同步 tasks/milestones 状态；清理验证素材。REQ-104/105/106/111 代码已实现，待真实环境（screenpipe/桌面 Win32/飞书凭据）验收 | `requirements.txt`、`.venv/`、`docs/tasks.md`、`docs/milestones.md`、`README.md`、`docs/m1-requirements.md` | T-101~111 / M1 |
| 2026-09-02 | 代码审查修复（9 项）：①飞书 SDK 用法重写（EventDispatcherHandler.builder + WsClient 关键字参数 + data.event.message/sender 取值 + message_id 去重；SDK 无 stop() 改 stop_event 标志）②inbox.write_material 全程持锁（去重检查+写文件+索引原子化）③采集基类每次 spawn 发放独立 stop_event（修复 stop→start 旧线程复活双跑），clipboard/screenpipe 循环改 stop_event.wait ④文件监听入库前稳定检测（0.5s 两次 stat 大小一致，防读半截文件）⑤frontmatter meta 改 yaml.safe_dump 转义（防窗口名/网页标题含冒号换行破坏结构）⑥网页抓取 iso-8859-1 时回退 apparent_encoding（防中文乱码）⑦Config 深拷贝 DEFAULTS（防写穿模块级默认）⑧剪贴板启动先读基线（启动前旧内容不入库）⑨控制台加 Host/Origin 本机校验中间件（挡跨站 multipart/simple POST，非本机 403）。回归全通过：模块导入、20 线程并发写入去重、stop_event 生命周期、稳定检测、TestClient+真实 uvicorn 冒烟（含 evil Host/Origin 403） | `app/capture/feishu_source.py`、`app/capture/inbox.py`、`app/capture/base.py`、`app/capture/file_source.py`、`app/capture/clipboard_source.py`、`app/capture/screenpipe_source.py`、`app/capture/web_source.py`、`app/config.py`、`app/web/server.py` | 代码审查修复 / T-102~110 |
| 2026-09-02 | 运行期崩溃修复：真实桌面首次运行即崩，faulthandler 定位 `read_clipboard_text` Access Violation —— Win32 调用未声明 argtypes/restype，64 位下句柄被截断成非法指针，GlobalLock 后读内存崩溃。改为显式原型声明 + `wstring_at` 读取，沙盒实测成功读取真实剪贴板（此前"沙盒限制"结论系误判，实为同一 bug）；`run.py` 增加 faulthandler.enable 与异常兜底，窗口不再闪退 | `app/capture/clipboard_source.py`、`run.py` | T-105/T-111 验证 |
| 2026-09-02 | 网页抓取体验改进：实测确认火山方舟等 SPA 文档站为 JS 动态渲染，纯 HTTP 抓取正文为空（设计边界，REQ-108 静态抓取）。URL 输入容忍 markdown 反引号；空正文时区分提示"疑似 JS 渲染或需登录，建议手动导入"。静态页面不受影响 | `app/capture/web_source.py` | T-108 |
| 2026-09-02 | 扩展点记录与 screenpipe 提示完善：tasks.md 新增"扩展点备忘"（T-113 screen 源 meta 记录 speaker 说话人，支撑"谁说的"溯源）；`screenpipe_source.py` 未安装提示给出具体安装命令（winget/官方应用/npx），docstring 修正为"官方以桌面应用或 CLI 分发，无 Python 库形态" | `app/capture/screenpipe_source.py`、`docs/tasks.md` | T-104/T-113 |
| 2026-09-02 | 采集方案变更（ADR-014 取代 ADR-003）：screenpipe（Rust 外部服务，无 Python 库形态）改为 **VibeVoice-ASR-BitNet** 本地转写 —— 微软 2026-07-23 开源（MIT），VibeASR.cpp 引擎 + BitNet 三值量化 1.5B 解码器，模型 4.62→1.58GB，CPU 4 线程实时（RTF<1）无需 GPU，项目以子进程调用 `asr_infer` CLI 集成；屏幕画面 OCR 不在其能力内，按用户决策移出 M1 范围；inbox 源 `screen`→`meeting`。代价与风险：较 7B 版 WER 退化 1-4 个百分点（中文会议 AliMeeting 40.58 偏高，质量待实测）、Windows 构建需 MinGW-w64（MSVC 不支持）、说话人/时间戳字段是否保留待实测（T-113）。文档同步：ADR-014 新建 + ADR-003 标注 superseded；REQ-104 重写、T-002/T-104/T-113 更新（T-104 改回 todo 待按新方案实现）、依赖/配置表/source 集合/architecture/milestones/prd 对应更新；代码侧 `screenpipe_source.py` 等待后续重写 | `docs/adr/ADR-014-vibevoice-asr.md`、`docs/adr/ADR-003-screenpipe.md`、`docs/tasks.md`、`docs/m1-requirements.md`、`docs/architecture.md`、`docs/milestones.md`、`docs/prd.md` | T-002/T-104/T-113 / ADR-014 |
| 2026-09-02 | T-104 代码重写落地（screenpipe 源 → meeting 转写源）：删除 `screenpipe_source.py`，新建 `meeting_source.py`（子进程调用 asr_infer，参数 `--vae-model/--lm-model/--audio/-t 4`；drop 热文件夹轮询扫描 + 稳定检测；`.meeting_state.json` 状态文件防重复入库（成功才记录，失败本会话不重试、重启自动重试）；转写失败不落空文件；引擎/模型未就绪 → 状态 error 带构建/下载提示，不影响其他源；Windows CREATE_NO_WINDOW 防控制台闪窗；输出 JSON→代码块/纯文本原样，schema 待实测 T-113）。配套：manager 注册、inbox SOURCES `screen`→`meeting`、config DEFAULTS+config.yaml（`sources.meeting.enabled/engine_path/model_dir`）、设置 API 与前端开关（setMeeting"会议音频 (VibeVoice-ASR)"）、requirements.txt 注释改为外部可选组件说明。烟雾测试 5 项通过（源注册/未就绪 error 降级/伪造引擎+gguf 就绪判定/设置 API 开关闭环/输出格式化），config.save 丢注释为既有行为 | `app/capture/meeting_source.py`（新建）、`app/capture/screenpipe_source.py`（删除）、`app/capture/manager.py`、`app/capture/inbox.py`、`app/config.py`、`config.yaml`、`app/web/server.py`、`app/web/static/index.html`、`app/web/static/app.js`、`requirements.txt`、`docs/tasks.md` | T-104/T-002 / ADR-014 / REQ-104 |
| 2026-09-02 | T-104 引擎构建完成：VibeASR.cpp 经 MinGW-w64（WinLibs GCC 16.1.0 UCRT）+ CMake 4.4.3 构建出 `tools/vibeasr/build/bin/asr_infer.exe`（3.0MB），llama.cpp 子模块（fork 分支 vibeasr-dev + kompute 嵌套子模块）经 ghfast 镜像递归拉取；`engine_ready()` 返回 True（引擎 + 模型检查全通过）。踩坑记录：① WinLibs 自带 ccache 在本机 Windows 并行编译时死锁（9 个 ccache 进程冻结），以 `-DGGML_CCACHE=OFF` 禁用后一次通过；② MinGW 生成器须按 README 用 `MinGW Makefiles` + `mingw32-make`，仅构建 `--target asr_infer`；③ 运行时依赖 MinGW bin 在 PATH（用户环境已具备）。待办：真实会议录音验收 | `tools/vibeasr/build/bin/`、`docs/tasks.md` | T-104/T-002 / ADR-014 / REQ-104 |
| 2026-09-02 | llm_wiki 移植决策（ADR-015）：调研 nashsu/llm_wiki（GPL v3，Karpathy LLM Wiki 模式，Tauri+React19+TS）后经用户确认两项决策——①分发计划 = 个人自用不分发（GPL 义务不触发，可移植其代码）；②展示层 = 控制台升级 React 19+Vite+TS 直接移植组件（修订 ADR-010 前端部分，FastAPI 不变）。新建 ADR-015：M2 编译层移植 `src/lib/` ingest 内核（两步 CoT/持久化队列/SHA256 缓存/dedup/context-budget/CJK 清洗/llm 路由/lint/review）为 Python `app/compile/`，保留 KnowSeq 自有部分（inbox 适配、四类条目 schema）；图谱组件 sigma.js+graphology+ForceAtlas2；插件参考 `extension/` 改接 FastAPI；合规边界：参考源码本地化 `reference/`（.gitignore 已排除）、移植文件注明 Ported from + GPL-3.0 归属、未来分发前须重评估。tasks.md：T-009/T-011 补 ADR-015 关联并更新描述，新增移植待办 T-114/T-115/T-116，下一步 2 更新；ADR-008/ADR-010 补修订说明 | `docs/adr/ADR-015-llm-wiki-port.md`（新建）、`docs/adr/ADR-008-compile-layer.md`、`docs/adr/ADR-010-frontend.md`、`docs/tasks.md`、`.gitignore` | T-009/T-011/T-114~116 / ADR-015 |
| 2026-09-02 | 参考源码本地化：经 ghfast.top 加速镜像克隆 nashsu/llm_wiki 到 `reference/llm_wiki`（v0.6.11，--depth 1；本机无代理可用，用户确认走加速镜像路线；清理上次直连失败的残留 .git 后重试成功）。git check-ignore 验证 reference/ 已被忽略，不进仓库、不随项目分发（ADR-015 合规边界）。src/lib 下 227 个源文件、extension/、mcp-server/、src-tauri/ 齐备，满足 T-114/T-115/T-116 移植所需 | `reference/llm_wiki/`（克隆，git 忽略）、`docs/tasks.md` | T-114~116 / ADR-015 |
| 2026-09-02 | T-104 模型就位：VibeVoice-ASR-BitNet gguf 下载完成（vibeasr-vae-encoder-i8_s.gguf 670.51MB + vibeasr-lm-i2_s-embed-q6_k.gguf 946.88MB，`models/vibeasr/`），`engine_ready()` 验证模型检查通过，仅剩引擎可执行文件待 MinGW-w64 构建。下载渠道波折记录：HF 直连慢 → hf-mirror 镜像起步正常后 stall（双进程抢锁清理后仍 0 字节，疑似 Xet 仓库回退 CDN 被墙 + 镜像晚高峰过载）→ 切换 ModelScope（同仓库文件齐全，实测 15~20MB/s 并行）成功。VibeASR.cpp 引擎源码已克隆 `tools/vibeasr/`（ghfast 镜像）。环境备注：venv 新增 huggingface_hub/hf_transfer/modelscope（下载工具，非运行时依赖） | `models/vibeasr/`（模型，git 忽略）、`tools/vibeasr/`（引擎源码，git 忽略）、`docs/tasks.md` | T-104/T-002 / ADR-014 / REQ-104 |
| 2026-09-02 | **M1 真实环境验收全部通过（T-112 收尾，M1 核心完成）**：用户真实桌面验收通过——T-104 meeting 转写（真实录音拖入 drop 热文件夹 → `inbox/meeting/` 产出转写稿）、T-105 剪贴板自动入库、T-109 粘贴导入、T-110 控制台各页面（状态/素材/设置含"会议音频 (VibeVoice-ASR)"开关）、T-111 托盘左键开关采集。文档同步：tasks.md 任务状态批量 done（T-002/T-003/T-008/T-104/T-105/T-110/T-111/T-112），"下一步"节重写为 M2 启动 + 遗留补做；milestones.md M1 标记核心完成。唯一遗留：T-004/T-106 飞书联调待凭据（保持 doing）、T-113 待转写 schema 确认。下一里程碑：M2 编译层移植（T-009/T-114，参考源码/决策文档/移植清单全部就位） | `docs/tasks.md`、`docs/milestones.md` | T-112 / M1 |
| 2026-09-03 | **M2 需求拆分（REQ-201~214，移植前细致拆分）**：新建 m2-requirements.md（§0 范围/六条前置决策/裁剪清单/GPL 合规 + §1~§14 每 REQ 含 Python 接口签名/移植来源/适配点/验收 + §15 配置枚举 + §16 NFR + §17 待办映射）。关键决策固化：仅 OpenAI 兼容协议 + Provider adapter 三方法抽象（用户确认）；threading 并发不引 asyncio；预算字符制不引 tokenizer；四类条目中文 SCHEMA 内置常量；API Key 走 .env `LLM_API_KEY`、base_url/model 走 config.yaml；lint 断链只报告不建 stub（KnowSeq 无 query 型）；review 裁剪 sweep/create-page/batch-research；dedup embedding 预筛默认关。tasks.md：T-009 转 doing，新增"M2 细分待办"表 T-201~214（覆盖 T-009/T-114），下一步节改为 M2 实施序 | `docs/m2-requirements.md`（新建）、`docs/tasks.md` | T-009/T-114 / T-201~214 / M2 |
| 2026-09-03 | ADR-008 修订：新增第五类条目**疑问（Query）**——落 `knowledge/queries/`，frontmatter 加 `status: open/resolved`，解决后转决策/教训/概念并 related 互链；原四类无法承载"开放问题/矛盾追踪"，为 llm_wiki 九类页型中唯一真正缺失的页型。连带影响：lint 断链恢复自动建 type:query stub（原"只报告不建 stub"决策作废，m2-requirements P6/REQ-209 更新）；五类 SCHEMA 内置常量（REQ-207）；index 按五类分区（REQ-208）。文档全量同步：ADR-008/015、m2-requirements（13 处）、tasks.md、architecture.md、M2 规划文档 D1/假设 5/T-214 | `docs/adr/ADR-008-compile-layer.md`、`docs/adr/ADR-015-llm-wiki-port.md`、`docs/m2-requirements.md`、`docs/tasks.md`、`docs/architecture.md`、`.trae/documents/m2-compile-layer-requirements-port-plan.md` | ADR-008 修订 / M2 |
| 2026-09-03 | **B1 基础层完成（T-201~204，REQ-201~204）**：①`app/compile/` 包骨架 + config `compile.*` 配置节 + `knowledge/` 五类目录（含 queries/）与 index.md/log.md 初始化（materials.py：frontmatter 解析 / is_compiled / mark_compiled 回写）；②LLM 客户端 llm_client.py（OpenAI 兼容 requests SSE 流式、`data:`/`[DONE]` 解析、reasoning 内容检测、429→RateLimitedError、去掉温度参数重试、has_usable_llm 门禁，Key 读 .env `LLM_API_KEY` 不落日志）；③sanitize.py 四步修复 + filename.py slug 命名（**修正：text-chunker.ts 系 llm_wiki 死代码不移植，长源语义分块 splitSourceIntoSemanticChunks 归 T-208**，REQ-203/m2-requirements §3 已回写）；④context_budget.py 字符制预算（DEFAULT 204800/15%/5%/50%/30%+5000 下限）。验收全部通过（含移植 fixtures），临时脚本已清理 | `app/compile/__init__.py`、`app/compile/materials.py`、`app/compile/llm_client.py`、`app/compile/sanitize.py`、`app/compile/filename.py`、`app/compile/context_budget.py`、`app/config.py`、`docs/m2-requirements.md`、`docs/tasks.md` | T-201~204 / M2 / B1 |
| 2026-09-03 | **B2 幂等与队列完成（T-205/206，REQ-205/206）**：①cache.py IngestCache（Ported 自 ingest-cache.ts）——双条件命中（SHA256 相同且 files_written 全部仍存在）、损坏缓存视为空、写失败静默、`.knowseq/compile-cache.json`；②commit.py CommitCoordinator（Ported 自 ingest-commit-coordinator.ts）——Promise 链以 threading.Condition + 序号票据等价实现"准备并发、落盘 FIFO 串行"，reserve 按任务启动序发票据、release 幂等；③queue.py CompileQueue（Ported 自 ingest-queue.ts 简化版，单项目）——持久化 `.knowseq/compile-queue.json`（过滤 done、processing→pending kill 恢复）、MAX_RETRIES=3、429 全局暂停 15min 自动恢复（兄弟 processing 全部回 pending）、upsert(revive) 复活语义（自动轮询 revive=False 防失败任务无限复活，手动 revive=True）、内置 1-5 worker 线程池、_yanked 防 pause/cancel 竞态、stop 可重启续跑；④manager.py 接入 CompileQueue+IngestCache（_run_task 为队列 runner、_pipeline 留 T-207 接入点、缓存命中直接推进标记）；`.gitignore` 加 `.knowseq/`。验收 32/32 通过（缓存双条件/并发 FIFO/upsert 状态机/失败 3 次/重启恢复/429 暂停恢复/暂停恢复/Manager e2e），临时脚本已清理。**接口实现偏差**（按源 keying 更实用）：`get/put` 带 source_path、`upsert(source_path, revive=True)`、队列新增 retry/cancel/resume/summary。**修复**：fail/cancel/_pause_locked/stop 清理 _runs（手动驱动路径防 take 永久跳过）；LLM 门禁移到缓存 miss 之后（缓存命中无需 LLM 配置） | `app/compile/cache.py`（新建）、`app/compile/commit.py`（新建）、`app/compile/queue.py`（新建）、`app/compile/manager.py`、`.gitignore`、`docs/tasks.md` | T-205/206 / M2 / B2 |
| 2026-09-03 | **B3 提炼核心完成（T-207/208，REQ-207/208）**：①schema.py 五类中文 SCHEMA/PURPOSE 内置常量 + CATEGORIES/CATEGORY_BY_TYPE（九类→五类路由）；②blocks.py parse_file_blocks（CRLF 归一、围栏状态机内 closer 不生效、opener `---FILE: path---` / closer `---END FILE---`、is_safe_entry_path 路径安全、未闭合安全路径计入 truncated_paths 供修复）+ parse_review_blocks（ReviewBlock.options 缺省 ["Approve","Skip"]）+ filter_repair_blocks（块级返回 kept+warnings，按模型请求路径过滤）+ should_run_dedicated_review_stage；③long_source.py analyze_long_source（逐块 temp 0.1/max 4096 分析 + "Updated Global Digest" 滚动全局摘要，摘要精简去 Entities 节；checkpoint 参数全等校验续跑；system prompt 标记"长文档素材"）+ 长源预算/分块参数（context_budget.py 增 compute_source_budget/compute_chunk_params）；④indexer.py update_index（无变化不写盘返回 False）/append_log（幂等重复行跳过、空文件初始化、追加尾换行归一）；⑤pipeline.py compile_one 九步主流程（mkdir→长源分支→Step1→Step2→dedicated review（失败吞掉、取消上抛）→_write_blocks 落盘三分支→截断修复一次（recovered 按模型请求路径匹配 + 截断警告前缀移除）→确定性 index/log 兜底 + 无条目页 fallback 摘要→完整性门禁）+ 四组中文 prompt（分析/生成/审查建议/截断修复，输出格式末节 + 语言指令首尾重复）+ _stamp_entry（date 对象 + _NoAliasDumper 禁 alias，防 yaml 日期引号与 `&id001`/`*id001` 锚点产物）+ 解析警告并入 _WriteResult（对齐原文 1901-1902）；⑥manager.py 接线：_pipeline 构造 PipelineContext→compile_one，CompileIncompleteError 上抛→队列 fail 重试语义，CompileCancelled 捕获 return []→_yanked 丢弃不计失败，warnings 经 _last_warnings（锁保护）+status() 暴露（项目无 logging 设施）；__init__.py docstring 修正为五类。**移植适配偏差**：完整性门禁以 CompileIncompleteError 异常替代原文 ok/err 双布尔；语言守卫 zh 简化为 CJK 占比≥0.15 启发式（原文 detectLanguage 库）；config.yaml 空键时 Config.get 返回 None 的语义以 `or 默认值` 兜底；prompt 组装按非空行 join（if line 过滤条件空段占位）。**修复**：产物 files 基准统一为相对 knowledge_dir——原文条目路径带 `knowledge/` 前缀而 cache.files_written 存在性检查按 root=knowledge_dir 解析，混用导致缓存永不命中（B3 集成验证发现）。验收：pipeline 33/33（token 分档/短源 happy/语言守卫 fallback/截断修复成功/门禁 raise/长源 checkpoint 续跑只分析剩余块）+ manager 集成 20/20（happy path/缓存命中不调 LLM/取消丢弃/门禁透传/修复成功警告移除），临时脚本已清理 | `app/compile/schema.py`（新建）、`app/compile/blocks.py`（新建）、`app/compile/long_source.py`（新建）、`app/compile/indexer.py`（新建）、`app/compile/pipeline.py`（新建）、`app/compile/context_budget.py`、`app/compile/manager.py`、`app/compile/__init__.py`、`docs/tasks.md` | T-207/208 / M2 / B3 |
| 2026-09-03 | **B4 质量闭环完成（T-209/210，REQ-209/210）**：①lint.py（Ported 自 lint-structural-core.ts + lint-fixes.ts + lint.ts 页面构建段）——lint_knowledge(knowledge_dir)→list[LintIssue{kind,file,detail,suggestion,severity,broken_target}]，纯函数核心 suggest_link_targets/normalize_target/levenshtein/string_similarity/fragments（NFKC bigram 去重保序）；阈值常量全对齐原文（断链建议 0.74、关联建议 0.08+同目录 bonus 0.08、CJK 单字权重 0.35、同名 0.96、互相包含 0.82、候选上限 64、token 窗口 4000）；检查项：broken-link/orphan/no-outlinks + 新增 duplicate-name(warning)/duplicate-candidate(info)（标题归一判重，对应原 duplicate 检查语义）；修复组：append_wikilink（Related 节首插入/尾部建节/已有链接跳过）、rewrite_wikilink_target（保 alias）、create_stub（断链无建议时自动建 type:query stub 落 queries/{slug}.md，status: open，幂等复用）；ignore_pages 仅按 slug+basename 匹配（"foo" 不屏蔽 "archive/foo"），ignored 页出链不查；semantic 参数门控 relatedCandidate（默认关，对应 config compile.lint.semantic），Levenshtein 断链建议恒开；**适配差异**：stub 一律落 queries/（弃原子目录路由，防违反 type-目录一致规则）、knowledge/ 前缀复用 blocks.ENTRY_DIR_PREFIX、LLM 语义 lint 不移植、detail 中文化。②review_store.py（Ported 自 review-store.ts + review-utils.ts）——ReviewStore(base_dir) JSON 持久化 `.knowseq/reviews.json`（与 cache.py 同款线程安全范式），add/list/resolve；review_id_for 内容派生稳定 id `type::normalize_title(title)`（REQ 改动：原文 FNV-1a 哈希改直接拼接）；normalize_review_title 剥中英七前缀（missing/duplicate-page/possible-duplicate/缺失页面/缺少页面/重复页面/疑似重复）；合并语义双轨：add 走 incoming detail 优先（prefer_new=True）、load 折叠走首见优先+最早 created+resolved 胜出（对齐原 normalizeReviewItems 迁移语义，旧哈希 id 自动迁移）；resolve 后重加不回退 resolved；扩展 resolved_note 记处理备注。③manager.py 四处接线：import ReviewStore/from_block、__init__ 挂 self.reviews、_pipeline 内 outcome.reviews 入库（source_path 记素材路径）、status() 暴露 reviews_open。验收：lint 12/12（typo 建议/5000 页 0.124s/ignore 三态/rewrite 保 alias/append 三态/stub 幂等+路由/阈值边界/normalize_target/semantic 门控/端到端五类发现）+ review 40/40（中英前缀/id 稳定性/add 合并双轨语义/resolve 三态/跨实例持久化/旧格式迁移容错/manager 直调 _pipeline reviews.json 落盘+reviews_open+二次编译去重），临时脚本已清理 | `app/compile/lint.py`（新建）、`app/compile/review_store.py`（新建）、`app/compile/manager.py`、`docs/tasks.md` | T-209/210 / M2 / B4 |
| 2026-09-03 | **B5 第一部分完成（T-211，REQ-211）**：dedup.py（Ported 自 dedup.ts + dedup-runner.ts + dedup-storage.ts + sources-merge.ts）——三阶段 API：extract_entity_summary（五类全扫，原版仅 entities+concepts；description=frontmatter 优先/正文首段回退跳 # 与 \| 行/截断 200）→ detect_duplicate_groups（<2 不调 LLM；>250 无预筛直接 []（原版 ≤250 全扫语义，防挂起）；80 条/批 + overlap 8（stride=72，按 title\0slug 排序切片，_unique_groups 聚合去重）；DETECTOR temperature 0.1/max 8192；_parse_detector_response 状态机提取首个 JSON 对象（reason 内花括号存活、坏 JSON/非 dict/非 list 容错 []）；无效 slug 过滤 + not-duplicates 白名单过滤）→ merge_duplicate_group（slugs<2/canonical 不在组 ValueError，canonical_slug 可选键缺省 slugs[0]；MERGER temperature 0.1/max 16384；sources/tags/related 并集（existing 在前、大小写首见胜出）+ updated 盖章 ctx.now；跨页 wikilink 重写保 alias + related 重写去重；备份 .knowseq/page-history/dedup-{stamp}/ 全部被改文件原文；删除被并页；indexer.update_index 全量重建 index 替代原版保守删行；空 LLM 输出守卫 ValueError 防写空文件毁数据）。白名单持久化 .knowseq/dedup-not-duplicates.json（canonical key lower+sort 逗号连接，add 幂等）。embedding 预筛钩子 _embedding_prefilter_pairs 预留默认关。纯计算核心 _merge_group_core 独立可测（I/O 归属 _persist_merge），六条适配差异已注文件头。验收 80/80 通过（extract/parse/JSON 提取/detect 含白名单与 >250 防挂起/批扫切片 [80,80,56]/白名单持久化/重写 8/标量 3/数组工具 10/校验/core 13/E2E 主验收：paos+聚磷菌 检出→合并一条→sources/related/tags 并集保留→跨页引用重写保 alias→index 重建→备份 3 文件→二次合并报错），临时脚本已清理 | `app/compile/dedup.py`（新建）、`docs/tasks.md` | T-211 / M2 / B5 |
| 2026-09-03 | **B5 完成（T-212，REQ-212）**：enrich.py（Ported 自 enrich-wikilinks.ts）——`enrich_wikilinks(knowledge_dir, ctx, *, dry_run=False)→EnrichResult`；遍历五类条目（原版为单页保存钩子）每页一次 LLM 调用（temperature 0.2/max_tokens 2048 显式固定），LLM 只返回 `{"links":[{"term","target"}]}` 替换清单（原版 v2 设计：模型不重写正文，正文在 [[ ]] 之外字节不变）；`_validate_links` 归一 target（剥 alias/锚点→取文件名→去 .md→NFKC+lower）必须命中磁盘权威 slug 才保留（大小写/Unicode 等价的歧义键剔除）、改写 canonical 形态；`_apply_links`/`_find_unlinked` 每 target 只链一次、term==target（大小写不敏感）插 `[[term]]` 否则 `[[target|term]]`、跳过已在 `[[...]]` 内的出现；正文新插入 targets 并入 frontmatter related（复用 dedup.py 同源工具 `_parse_frontmatter_array`/`_merge_lists`（existing 在前）/`_write_frontmatter_array`），保证 related 与正文链接一致且目标均存在（REQ-212 验收）；dry_run 只报告不写任何文件；索引仅作 prompt 上下文、磁盘扫描为唯一权威边界（模型编造 target 一律丢弃）。验收 41/41 通过（归一 6/slug 歧义 2/index 上下文 1/parse 8/validate 3/apply 6 + dry_run E2E 三页变更报告文件零改动 + 真实运行主验收：三页 [[ ]] 插入与 related 追加/旧值保留、Ghost 编造目标未落盘、全页链接目标 ⊆ 磁盘 slug 集 + 空库 scanned=0 零 LLM 调用），临时脚本已清理。**修复**：`_find_unlinked` 修正原版潜在缺陷（原版仅查 idx 前 2 字符是否 `[[`，拦不住已有链接 alias 段内出现，会生成嵌套 `[[target|[[term]]]]`；本版判断 idx 是否落在 `[[` 与其 `]]` 区间内）；自链排除为评审加码，验收确认后回归源码语义移除 | `app/compile/enrich.py`（新建）、`docs/tasks.md` | T-212 / M2 / B5 |
| 2026-09-03 | **B6 控制台集成完成（T-213，REQ-213）**：server.py `create_app(config, inbox, manager, compile_mgr=None)` 新增 compile/knowledge 8 路由——`GET /api/compile/status`（queue 摘要+llm_ready+last_scan/error/warnings+reviews_open）、`POST /api/compile/trigger`（body `{scope:"all"|"path", path?}`，all→trigger_all 复活停摆任务计数、path→trigger_path 单条入队）、`GET /api/compile/reviews`（open 列表）、`POST /api/compile/reviews/resolve`（`{id,note?}` 不存在 404）、`GET /api/knowledge`（五类条目列表；`?path=` 读单条目，resolve 后必须落在 knowledge 子树内否则 404 防穿越）、`POST /api/compile/lint`/`dedup`/`enrich`（enrich 支持 `?dry_run=true`；三端点 try/except 兜底，LLM 未配置返回 400 细节而非 500）。manager.py 新增触发器：`trigger_path`（scope=path 入队）、`run_lint`（asdict 序列化 LintIssue）、`run_dedup`（extract→detect 批扫→逐组合并，单组失败跳过不阻断，cancel_event 可中断）、`run_enrich`（dry_run 只报告不写盘）。settings GET/POST 扩展 compile 节（enabled/auto/llm_base_url/llm_model；Key 仍走 .env 不入请求）。前端 index.html+app.js 新增四块卡片：编译状态（enabled/LLM 就绪/队列计数/限流暂停/reviews_open/pending 列表/last_error-warnings）、编译触发（全部/单路径入队）、Review 待处理（列表+备注+解决，事件委托）、知识库浏览（按类分组按钮→内容 pre）、手动工具（lint/dedup/enrich-dry/enrich 结果 pre 展示）；设置卡加 compile LLM 配置输入。main.py 接入：创建 CompileManager 并 start（compile.enabled 门禁）、传入 create_app。验收 21/21 TestClient 冒烟（status 字段/空库→条目列表/单条内容/穿越 404/不存在 404/lint broken-link/trigger all+path+pending 反映/review 加入-解决-404-归零/dedup 单页 detected=0/enrich 无 LLM 400 提示/无 compile_mgr 404），临时脚本已清理 | `app/web/server.py`、`app/web/static/index.html`、`app/web/static/app.js`、`app/compile/manager.py`、`app/main.py`、`docs/tasks.md` | T-213 / M2 / B6 |
| 2026-09-03 | **B7 M2 端到端验收完成（T-214，REQ-214，M2 关闭）**：真实 LLM（火山方舟 `deepseek-v4-flash`，base_url/model 落 config.yaml、Key 走 .env）全链路验收 31/31——3 条真实素材（剪贴板坑/条目组织/上下文预算）入 inbox → CompileManager 手动触发 → 产出 9 条五类条目（lesson/concept/query，frontmatter type 合法、sources 引用素材路径、index.md/log.md 维护、素材 compiled 回写）→ 幂等（二次 trigger 入队 0）→ 缓存命中路径（预置缓存素材直接标记不调 LLM）→ lint（broken-link 检出）/dedup（批扫合并）/enrich（dry_run 真实 LLM）→ 控制台（llm_ready=True、knowledge 列表、reviews 可用）。**端到端发现并修复**：enrich 在 dsv4flash 下逐页长思考导致 300s 读超时（探针证实 reasoning_tokens 数百~上千）——`llm_client._apply_reasoning` 原仅对 `api.deepseek.com` 关 thinking，泛化为"未启用 ingest_reasoning 且模型匹配 `deepseek-v[4-9]` 即发 `thinking:{type:disabled}`"（火山 ark 实测接受且提速 3.7s→1.4s），移除未用 urlsplit import。T-009/T-114/T-214 全部 done，M2 里程碑关闭，M3（T-010 LightRAG）启动 | `app/compile/llm_client.py`、`app/config.py`、`config.yaml`、`.env`、`docs/tasks.md`、`docs/milestones.md` | T-214 / M2 / B7 |
| 2026-09-03 | **M3 需求拆分（REQ-301~306，里程碑文档落地）**：新建 m3-requirements.md（§0 六条前置决策 + §1~6 REQ-301~306（引擎封装/索引/检索/问答/图谱/API）+ §7 brain 配置枚举 + §8 整体验收 + §9 待办映射）；tasks.md 新增"M3 细分待办"表 T-301~306（T-010 doing）；milestones M3 标记进行中。关键决策固化：**D1 LightRAG asyncio 内部化、对外同步门面**（专用后台事件循环线程 + run_coroutine_threadsafe，不改变采集/编译层 threading 风格）；D2 lightrag-hku 依赖（M3 唯一新增第三方依赖）+ 默认本地存储（JsonKV+NanoVectorDB+NetworkX 不引外部服务）；D3 embedding/LLM 走 OpenAI 兼容（embedding 独立 base_url/model，问答复用 compile.llm）；D4 索引增量幂等（rel 路径 doc_id + 内容哈希）；D5 图谱导出 vis-network 兼容 JSON（nodes/edges）；D6 Key 复用 .env LLM_API_KEY。裁剪：交互层图谱可视化/问答 UI（M4/T-115）、多模态、LightRAG Server、高级存储均不在 M3。调研依据：LightRAG 1.5.x（2026-05）asyncio API、openai_embed 兼容端点、自动实体抽取建图 | `docs/m3-requirements.md`（新建）、`docs/tasks.md`、`docs/milestones.md` | T-010 / M3 |
| 2026-09-03 | **M3 代码落地（T-301~305，REQ-301~306）**：①安装 lightrag-hku 1.5.7（nano-vectordb/networkx/numpy/tiktoken 等，Python 3.14 全 wheel）；config 新增 `brain:` 节（embedding.base_url/model/dim、llm 缺省复用 compile.llm）②`app/brain/engine.py` BrainEngine 同步门面——专用后台事件循环线程（D1，run_coroutine_threadsafe 提交、`_run_safe` 兜底）、start/stop/is_ready/status、LightRAG 构造（EmbeddingFunc 包装 openai_embed + dim 推断、llm_model_func 复用 openai_complete）③index 增量幂等（五类条目 rel 路径 doc_id + 内容哈希 → `index_state.json`；full_rebuild 清空工作目录全量重建；`index_state` 原子写）④search（naive `aquery_data` → hits 带 file_path）/ask（`aquery_llm` → answer+contexts 来源映射）⑤graph（`get_graph_labels` + `get_knowledge_graph` → nodes/edges JSON）⑥server brain API 5 端点：`GET /api/brain/status`、`POST /api/brain/index`（rebuild）、`POST /api/brain/query`（mode/top_k）、`GET /api/brain/search`、`GET /api/brain/graph`；`_require_brain` 400 兜底、brain_mgr None → status 404；前端"大脑"卡片（状态 chips/重建与增量索引/问答/检索/图谱 JSON 预览）；main.py 接线 BrainEngine（brain.enabled 门禁 start）。**修复**：status() 持锁调用 is_ready() 触发非重入 Lock 二次 acquire 死锁（改为无锁字段读）。验收：T-301~304 引擎 13/13（生命周期 start 幂等/status/索引首次 2→二次 0→变更只重插 1/检索命中带来源/问答 answer+contexts/图谱结构/stop） + T-305 API 13/13（无 brain_mgr 404、未就绪各端点 400、fake 就绪全通路 增量/重建/query/search/graph），临时脚本已清理。**已知现象**：stop() 关闭事件循环时 LightRAG 内部 worker 线程产生 asyncio 关闭噪音（非致命，进程正常退出，暂不处理） | `app/brain/engine.py`（新建）、`app/brain/__init__.py`（新建）、`app/web/server.py`、`app/web/static/index.html`、`app/web/static/app.js`、`app/main.py`、`app/config.py`、`config.yaml`、`requirements.txt`、`docs/tasks.md` | T-301~305 / M3 |
| 2026-09-03 | **M3 端到端验收完成（T-306，REQ-306 §8，M3 关闭）**：用户无 embedding API → **改本地真语义模型**（决策）：安装 torch 2.14 CPU + sentence-transformers 6.0.1 + transformers 5.16.1，ModelScope 下载 BAAI/bge-small-zh-v1.5 到 `models/embed/`（git 忽略，~/100MB）；`brain.embedding.mode=local`（config.yaml 落地，model_dir 相对 ROOT 解析）；engine 本地分支（SentenceTransformer 经 to_thread 加载、`get_embedding_dimension` 兼容新 API、local_embed async 包装、dim=512 推断）。问答/实体抽取复用云端 dsv4flash。**修复**：①LightRAG 1.5.7 自带 `openai_complete` 不接受 model 参数（从 hashing_kv.global_config 读 llm_model_name），partial 绑定造成 `openai_complete_if_cache() got multiple values for 'model'`——改自定义 `_make_llm_func` 直接调 `openai_complete_if_cache` 显式绑定 base_url/model/api_key；②`aquery_data` 返回顶层 `{status,data:{entities,relationships,chunks,references}}` 嵌套——`_extract_hits` 兼容 `data` 键。真实验收 19/19：local 引擎就绪（512 维）→ 首次索引 3→二次幂等 0→search("剪贴板采集有哪些坑") 命中 3 且含剪贴板条目来源（FR-020 语义检索）→ ask 非空答案+引用来源（FR-022）→ graph 31 节点/字段完整（FR-021）→ 控制台 status/index/query/search/graph 全端点。T-010/T-306 全部 done，M3 里程碑关闭，M4（T-011/T-115 React 交互层）待启动 | `app/brain/engine.py`、`app/config.py`、`config.yaml`、`requirements.txt`、`models/embed/bge-small-zh-v1.5`（git 忽略）、`docs/tasks.md`、`docs/milestones.md` | T-306 / M3 |
| 2026-09-03 | **M4 需求拆分（REQ-401~411，里程碑文档落地）**：新建 m4-requirements.md（§0 八条前置决策 + §1~11 REQ-401~411（工程骨架/设计系统/全局布局/编译页/问答页/知识库浏览/图谱页/素材与采集控制/知识管理/设置页/端到端验收与旧前端下线）+ §12 依赖与构建配置枚举 + §13 整体验收 + §14 待办映射）。关键决策固化：D1 React 19+Vite+TS（ADR-010 修订版）、FastAPI 无破坏性变更；D2 webui/ 源码 + 构建产物 app/web/dist 由 FastAPI 托管（dist 优先/static 兜底，验收后旧前端下线）+ vite dev 代理；**D3 图谱技术栈 sigma.js v3 + @react-sigma/core + graphology + forceatlas2 + louvain（修订 M3 D5"vis-network 兼容"表述，JSON 结构不变前端转换）**；D4 taste-skill（design-taste-frontend v2）设计系统先行（中密度/克制动效）；D5 移植合规 Ported from nashsu/llm_wiki (GPL-3.0)；D6 后端补齐素材标记/知识写删（brain.remove 索引一致性）/settings 扩展（llm_api_key 只写不回读）；D7 Host/Origin 校验沿用；D8 七页路由。tasks.md：T-011 转 doing，新增"M4 细分待办"表 T-401~411（覆盖 T-011/T-115），修正「下一步」过时记录（vis-network → sigma.js 系） | `docs/m4-requirements.md`（新建）、`docs/tasks.md` | T-011/T-115 / T-401~411 / M4 |
| 2026-09-03 | **T-401 前端工程骨架完成（REQ-401）**：新建 `webui/`（Vite 6 + React 19 + TypeScript strict + react-router-dom 7；Node 22.9.0 在 Vite 7 引擎要求（≥22.12）之外故取 Vite 6）——vite dev 代理 `/api`→FastAPI:8765、`build.outDir=../app/web/dist`（tsc --noEmit 类型检查通过，产物入库跟踪）；七页路由骨架（/ask /graph /knowledge /compile /materials /capture /settings + 404 兜底，NavLink 高亮）；server.py 静态托管改造：dist 优先（/assets 挂载 + SPA fallback catch-all，is_relative_to 防穿越、未匹配 /api/* 仍 404 不被 SPA 吞）+ 旧 static 兜底保留至 T-411 下线；.gitignore 增 `node_modules/`。验收：冒烟 8/8（dist index、/ask 与 /settings SPA 兜底、旧 /static/index.html 可访问、/api/status 200、/api/nonexistent 404、evil Host 403、evil Origin 403）+ vite dev 代理联调 200；临时脚本已清理 | `webui/`（新建：package.json、vite.config.ts、tsconfig.json、index.html、src/App.tsx、src/main.tsx、src/pages/×7、src/styles/base.css）、`app/web/dist/`（构建产物）、`app/web/server.py`、`.gitignore`、`docs/tasks.md` | T-401 / M4 / REQ-401 |
| 2026-09-03 | **T-402 统一设计系统完成（REQ-402/D4）**：按用户指令调用 taste-skill（design-taste-frontend v2 主力）——Design Read 定为 dark tech 工具风控制台，三档 dial 落定 `VARIANCE 3 / MOTION 3 / DENSITY 6`（工具型：可预测布局 + 克制动效 + 中等密度）；唯一 accent 绿 `#3ecf8e`（避开 AI 紫蓝）、单 dark 主题锁定、系统字体栈 + mono 数据字体、圆角两档（控件 6px/面板 10px）。落地 `webui/src/styles/` 三文件分层：tokens.css（CSS 变量：色板/字体/间距/动效时长/z-index）→ base.css（reset + 细滚动条 + focus-visible accent 环 + prefers-reduced-motion 全局关动画 + app-shell 布局壳 208px 侧导航/860px 移动端折叠）→ components.css（全站唯一组件来源：btn/chip/panel/table/field+input/switch/msg/empty/skeleton/toolbar/grid2/pre/md-body/wikilink）。App.tsx 品牌区多行化 + 404 页接入设计系统；main.tsx 引入 components.css。skill 范围说明：design-taste-frontend 明确 dashboard/dense product UI 不在其 landing-page 范围，自建 CSS 变量 tokens 不引重型组件库，anti-slop 原则与 dial 机制照常执行（已记入 m4-requirements D4 落定结论）。验收：`npm run build` 通过（tsc --noEmit + vite 6.4.3，CSS 10.00kB）+ dev server 浏览器 a11y 快照验证（七导航链接渲染/active 态正确/console 无错误） | `webui/src/styles/tokens.css`（新建）、`webui/src/styles/base.css`（重写）、`webui/src/styles/components.css`（新建）、`webui/src/main.tsx`、`webui/src/App.tsx`、`app/web/dist/`（构建产物）、`docs/m4-requirements.md`（D4 落定结论）、`docs/tasks.md` | T-402 / M4 / REQ-402 / D4 |
| 2026-09-03 | **T-403 全局布局与状态总览完成（REQ-403）**：①`webui/src/api.ts` 统一 fetch 封装（JSON 请求 + FastAPI detail 错误归一 ApiError）；②`webui/src/status/StatusContext.tsx` 全局状态轮询 Context——5s 轮询 `/api/status`、`/api/compile/status`、`/api/brain/status`（Promise.all 并行、404 归类 disabled 服务未启用、失败保留上次成功数据），类型对齐三个 manager 的 status() 返回结构，暴露 refresh()/indexBrain(rebuild)（POST `/api/brain/index` 成功后自动 refresh，返回 scanned/inserted/skipped 摘要）；③`webui/src/status/StatusRail.tsx` 侧边栏底部状态条——采集/编译/大脑三行 chip 语义色聚合（running/LLM 就绪+队列计数/review 数/限流暂停优先级/ready+last_error title 提示），采集编译行点击跳对应页，增量索引/重建按钮（大脑未就绪禁用）+ status-note 执行结果。样式入 components.css（status-rail 系列 + 860px 移动端横排适配）。**顺带修复 T-402 遗留**：App.tsx 导航链接缺 `<nav>` 包裹致 `.app-nav nav a` 样式未实际生效，补语义化 nav 元素。验收：真实服务栈（uvicorn 无托盘脚本，brain ready=true）+ 浏览器 a11y 快照——状态条真实反映三类状态（采集已停止/编译空闲/大脑就绪）、增量索引点击返回「增量索引完成：扫描 0，新增 0，跳过 0」且状态刷新、状态行点击跳转 /capture 且导航高亮切换、console 无错误；knowledge/ 目录为空（M3 验收数据已清理）故扫描 0 属实，T-411 验收时重建数据 | `webui/src/api.ts`（新建）、`webui/src/status/StatusContext.tsx`（新建）、`webui/src/status/StatusRail.tsx`（新建）、`webui/src/App.tsx`、`webui/src/styles/components.css`、`app/web/dist/`（构建产物）、`docs/tasks.md` | T-403 / M4 / REQ-403 |
| 2026-09-03 | **T-404 编译页完成（REQ-404）**：`webui/src/pages/CompilePage.tsx` 四区实现（覆盖旧编译面板全部能力）——①编译状态区：消费 StatusContext 共享轮询 `useStatus().compile`（不重复建请求），chips（启用/停用、LLM 就绪/未配置、限流暂停 warn）+ 四计数 stats（pending/processing/done/failed，failed>0 标红）+ last_error/last_warnings 提示 + pending 素材路径清单，disabled→空态「编译服务未启用」；②触发区：「全部重新入队」btn-primary + 单路径 input-mono 回车提交（triggered=0 → 错误提示「未入队：请填写 inbox/ 下的相对路径」，成功清空输入框）；③Review 区：5s 轮询 `/api/compile/reviews`（与全局轮询对齐、卸载清理），五列表格（类型 chip/标题/详情/来源路径/解决=备注输入+按钮），逐条解决 POST resolve（note 空传 null）成功后刷新+更新共享状态；④手动工具区：lint（issues 表格 severity chip + kind + file + detail + suggestion）/dedup（摘要 + merged JSON）/enrich-dry（预览未写盘）/enrich-run（写盘后 refresh），四工具 busy 互斥（busy: string|null 统一 disabled，解决类 `resolve:${id}` 细分）。样式追加 components.css（.msg-warn/.stats/.stat-num 变体/.plain-list/.review-detail/.review-resolve）。验收：`npm run build` 通过（tsc --noEmit + vite 6.4.3，CSS 11.99kB/JS 249.58kB）+ 真实服务栈浏览器验证——四区渲染正确、「lint 通过：未发现问题」（knowledge 空库 0 issues 属实）、「全部重新入队」→「已入队 15 条素材」且编译状态区与侧边栏状态条同步更新（待处理 0→15，inbox 实有 15 条：clipboard 11 + meeting 3 + web 1）；备注：a11y compact 快照过滤非交互文本节点（.msg 类验证以 browser_take_screenshot 截图为准）；验证后临时冒烟脚本已清理 | `webui/src/pages/CompilePage.tsx`（重写占位）、`webui/src/styles/components.css`、`app/web/dist/`（构建产物）、`docs/tasks.md` | T-404 / M4 / REQ-404 |
| 2026-09-03 | **T-405 问答页完成（REQ-405/FR-030）**：`webui/src/pages/AskPage.tsx` 双 tab 面板——①问答 tab：问题输入（textarea Enter 提交/Shift+Enter 换行）+ 检索模式 select（hybrid/local/global/naive/mix 五选项）+ 提问按钮（空输入/请求中禁用）→ POST `/api/brain/query`；回答 marked 渲染（`.md-body` 复用 T-402 设计系统）；**引用卡片列表**：contexts 渲染为可点击卡片，href 直连 `/knowledge?path={file_path}`（URL 编码），mode chip 显示当前检索模式 + 「清空对话」按钮（历史非空时）；②检索 tab：输入→GET `/api/brain/search`，hits 渲染为引用卡片同款链接（file_path 同样跳条目）；**四态门禁**（brain 未配置/未就绪→可操作提示不白屏、加载中 skeleton、就绪→面板）；**会话内存**：历史/命中存 module-level 变量（`memoryTurns`/`memoryHits`）——路由切换不丢、刷新即清空（Q&A 历史仅本页会话，明确 UI 提示）。**顺带修复后端来源路径 bug**：lightrag-hku 2.x 入队时 `normalize_document_file_path`（utils_pipeline.py）强制把 file_path 规范化为 basename，brain API 返回的 file_path 缺目录前缀导致引用跳转 `/knowledge?path=` 404——`engine.py` `_extract_hits` 来源解析链改为优先 `full_doc_id`（KG 路径 chunk 持久化记录含完整 rel 路径）、否则从 chunk_id 反解 `{rel}-chunk-NNN` 后缀（向量路径 chunk 仅存 chunk_id），存量索引数据无需重建；curl 验证 `?path=lessons/...` 200。种子数据 2 条（concepts/stable-file-detection.md + lessons/win32-clipboard-handle-truncation.md，含 wikilink/related 互链，`knowledge/` gitignore 排除不入库）+ 大脑索引，触发编译队列 15 条素材全跑完（产出 review 26 条、knowledge/ 扩至 30 条目，T-411 验收时重建索引）。依赖：package.json 新增 marked ^16（回答/条目 Markdown 渲染，T-406 知识库页复用；m4-requirements §12 已同步）。验收：`npm run build` 通过 + 真实服务栈（本地 embedding 512 维就绪）浏览器全量验证——①问答：提问→busy→回答 Markdown 渲染完整（h2 标题/有序列表/inline code/References h3）+ 引用卡片 URL 含完整相对路径、点击跳转 `/knowledge?path=concepts%2F...` 成功（占位页符合 T-406 前预期）；②mode 切换：naive 提问→mode chip 正确显示「mode: naive」；③检索 tab：「剪贴板 采集」→2 hits 链接 URL 完整路径正确；④console 无错误；验证后停服务、临时冒烟脚本已清理 | `webui/src/pages/AskPage.tsx`（重写占位）、`webui/src/styles/components.css`、`webui/package.json`、`webui/package-lock.json`、`app/brain/engine.py`（file_path 修复）、`app/web/dist/`（构建产物）、`docs/m4-requirements.md`（§12 依赖枚举补 marked）、`docs/tasks.md` | T-405 / M4 / REQ-405 |
| 2026-09-03 | **T-407 图谱页完成（REQ-407/FR-031）**：`webui/src/pages/GraphPage.tsx` 重写占位——`GET /api/brain/graph` → graphology Graph → sigma.js v3（@react-sigma/core v5 SigmaContainer）渲染。**移植合规**（Ported from nashsu/llm_wiki, GPL-3.0 头注）：`webui/src/lib/graph.ts`（buildGraph：节点/边构造 + Louvain 社区分配（graphology-communities-louvain）+ 社区/类型双色模调色板对齐设计系统 accent）、`webui/src/lib/graph-layout-worker.ts`（ForceAtlas2 Web Worker 布局，Ported 自 graph-layout-worker.ts——迭代分档 140/90/65/40/28 按 [220,500,1250,3000] 节点数选档，inferSettings+assign，positionCache 缓存收敛坐标）。页面结构：GraphLoader（主线程 <220 节点直接布局 / ≥220 Worker + 布局完成前 skeleton）、GraphRenderSettings（nodeReducer：选中 1.5x/悬停 1.4x/其余 mixColor 0.75 灰化；edgeReducer：hover/selected 邻接边 accent、dimmed 0.3 透明）、EventHandler（clickNode 选中聚焦/clickStage 清选/enterNode/leaveNode 悬停）、四态分支（未就绪/加载失败+重试/空图/加载中 提示齐全）、工具栏（搜索前 8 条命中→点击选中、按类型/按社区 tab 切换、+/−/⤢ 缩放三钮）、图例（随色模联动：类型 chip / 社区编号+计数）、详情面板（label/type/社区/度数 + DIRECTED 邻居按钮点击跳转选中）。依赖：package.json 新增 sigma ^3.0.3、@react-sigma/core ^5.0.6、graphology ^0.26、graphology-layout-forceatlas2 ^0.10.1、graphology-communities-louvain ^2.0.2（m4-requirements §12 已对齐实际版本并补偏差说明）。**三个真实 bug 修复（浏览器实测发现）**：①focusNode 相机坐标——sigma 相机 x/y 是归一化显示坐标 [0,1] 而非节点原始图坐标（本项目 0-100 空间），直传导致相机飞出视野白屏，改为 `sigma.getNodeDisplayData(id)` 换算（sigma 官方聚焦范式）；②edgeReducer 端点恒空——graphology 不把 source/target 存为边属性（`attrs.source/target` 恒 ''），高亮/压暗判断失效，改为 `sigma.getGraph().extremities(edgeKey)` 取回端点；③画布白底——GraphPage 引入的 `@react-sigma/core/lib/style.css` 定义 `--sigma-background-color:#fff` 且作用于 `.react-sigma` 根 div（sigma v3 无 backgroundColor 设置、各 WebGL 层 canvas 本身透明清除），components.css T-407 节覆盖 `.graph-canvas { --sigma-background-color: transparent; --sigma-controls-background-color: transparent; background: transparent }` 透出 `.graph-shell` 暗色面板底。验收：`npm run build` 通过（CSS 22.56kB/JS 517.02kB）+ 真实服务栈（.venv 内联 python -c 无托盘启动，工厂模式 create_app 不可 uvicorn 直引）浏览器全量验证（36 节点/48 边 ≥31 满足）——①36 节点 ForceAtlas2 收敛渲染流畅；②搜索「Open」命中→点击选中→详情面板（OpenClipboard·DIRECTED·社区 2·2 度）；③聚焦高亮正确（选中居中放大、邻居保留、accent 边、其余灰化）；④详情面板邻居按钮点击跳转 read_clipboard_text（9 度邻居全高亮）；⑤缩放三钮 +/−/⤢ 有效无报错；⑥刷新按钮重载图谱；⑦按类型/按社区着色切换 + 图例联动（社区 1×11/2×7/3×6/4×12 全覆盖 36 节点），切换后选中高亮逻辑保持；⑧`.graph-canvas` computed background=transparent（暗底恢复）；⑨console 无当前 bundle 错误（历史累积日志除外）。空图/未就绪提示为四态分支代码实现（brain 就绪状态无法自然触发，实现满足 REQ） | `webui/src/pages/GraphPage.tsx`（重写占位）、`webui/src/lib/graph.ts`（新建·Ported）、`webui/src/lib/graph-layout-worker.ts`（新建·Ported）、`webui/src/styles/components.css`、`webui/package.json`、`webui/package-lock.json`、`app/web/dist/`（构建产物）、`docs/m4-requirements.md`（§12 对齐）、`docs/tasks.md` | T-407 / M4 / REQ-407 |
| 2026-09-03 | **T-406 知识库浏览页完成（REQ-406/FR-031 前置）**：`webui/src/pages/KnowledgePage.tsx` 双视图——①列表：条目索引一次加载（GET `/api/knowledge`）+ 按文件名大小写不敏感搜索过滤 + 五类分组 panel（decisions/lessons/concepts/connections/queries，中文标签 + `cat · 计数`，空组自动隐藏）+ 刷新 + index.md/log.md 按钮；②详情：path 查询参数驱动（fetch `/api/knowledge?path=`，cancelled 守卫），**frontmatter 面板**（title/type chip/date、description、标签/关联/来源三行 chip——related resolve 成功为站内 Link、断链 dashed warn 提示、sources 统一跳 /materials，未知键进 extras 单调区）+ 正文 marked 渲染（`.md-body` 复用设计系统）。**移植合规**（Ported from nashsu/llm_wiki, GPL-3.0 头注）：`webui/src/lib/wikilink.ts`（`[[target]]`/`[[target|alias]]`→`#encodeURIComponent` 链接，``` 围栏+行内代码两级分段保护，裁剪 image embeds）、`webui/src/lib/knowledge.ts`（resolveWikiTarget 三形态：路径/裸 slug/.md 后缀，slug 全局唯一线性索引）、`webui/src/lib/frontmatter.ts`（不引 js-yaml 的简易 YAML 子集：kv/行式数组/`[a, b]`/引号剥离，逐行扫描定界）。**wikilink 渲染管线**：transformWikilinks→marked.parse→dangerouslySetInnerHTML→useEffect 后处理（`a[href^="#"]` resolve 成功加 `.wikilink`、失败加 `.wikilink-broken`+title 提示；外链 target=_blank）+ 容器 onClick 拦截 `#` 链接 preventDefault→resolveWikiTarget→路由跳转（EntryDetail 内经 pushState+popstate 派发同步 react-router）。样式：components.css 追加 T-406 节（kb-toolbar/kb-search/entry-grid/entry 卡片/kb-crumb/kb-path/fm 系面板/chip-sm/chip-broken/wikilink-broken warn 虚线下划线）；五类 chip 不移植 llm_wiki 多彩 icon 方案（单一 accent 设计锁定）。验收：`npm run build` 通过（CSS 15.91kB/JS 307.90kB）+ 真实服务栈浏览器全量验证（30 条目真实数据）——①列表五类分组正确（lessons·2/concepts·12/queries·15）；②搜索 vibeasr→4 条匹配；③条目详情 frontmatter 完整（title/type/date/标签/关联/来源，关联 chip 解析为 lessons 站内链接）；④正文 Markdown 渲染（h1/h2/table/list/inline code）；⑤wikilink 点击跳转双入口通过（正文+index.md→URL 切换+视图刷新）；⑥index.md 完整索引渲染（五类 heading+条目链接）；⑦返回列表按钮正常；⑧`.wikilink`=1/`.wikilink-broken`=0（本批数据无天然断链，断链样式类已确认入构建产物 CSS+JS）；⑨console 无当前 bundle 错误；验证后停服务、临时启动脚本已清理 | `webui/src/pages/KnowledgePage.tsx`（重写占位）、`webui/src/lib/wikilink.ts`（新建·Ported）、`webui/src/lib/knowledge.ts`（新建·Ported）、`webui/src/lib/frontmatter.ts`（新建）、`webui/src/styles/components.css`、`app/web/dist/`（构建产物）、`docs/tasks.md` | T-406 / M4 / REQ-406 |
| 2026-09-03 | **T-408 素材管理与采集控制页完成（REQ-408/FR-040）**：①后端——`inbox.py` 新增 `_read_compiled`（逐行扫 frontmatter 只认顶格 `compiled:`，不做整文件 yaml 解析）、`list_materials` 项扩展 `compiled` 字段、`_resolve_rel`（防穿越：is_absolute/`..` parts/resolve().relative_to 三重门，同 knowledge 模式）+ `read_material`（完整 frontmatter 解析返回 path/source/captured_at/compiled/meta/body 供预览）+ `mark_compiled`（锁内行级改写顶格 compiled 行、缺行在头定界前插入）；`manager.py` 新增 `start_one`/`stop_one`（未知源 ValueError→404）；`meeting_source.py` 覆写 `info()` 附加 `engine_ready`/`engine_hint`（经 /api/status 透出）；`server.py` 新增 `POST /api/materials/mark`、`GET /api/materials/content`、`POST /api/start/{name}`、`POST /api/stop/{name}`（**REQ-408 实现裁决**：验收「各源启停开关生效」需单源粒度而既有 API 仅全局启停，补最小增量端点，规格接口行已补记）。②前端两页——`MaterialsPage.tsx`：来源 tabs 筛选（全部/会议转写/剪贴板/飞书/文件/网页/手动导入）+ 头部统计（共 N·已提炼 X·待提炼 Y）+ 表格（来源 chip-sm/文件路径/大小/状态徽标 chip-ok|chip-off/预览+标记已提炼↔撤销标记，busyPath 互斥）+ 预览抽屉（backdrop 点击/ESC 关闭，fm-row 徽标+fm-date、fm-extras meta 键值、pre 正文 pre-wrap）；`CapturePage.tsx`：五源 src-card（src-name+状态 chip+meeting 引擎就绪 chip（title=engine_hint）+switch 开关）+ 全局启停 + 手动导入（标题/文本、空文本禁用 + 文件选择/上传 FormData 以 `headers:{}` 覆盖 api 默认 JSON Content-Type）+ 网页抓取（URL 输入回车/按钮）+ 飞书云文档导入；`toggleSource` 链路 = POST settings `${name}_enabled` 持久化 → POST `/api/start\|stop/${name}` → refresh（catch 亦 refresh 回读真实状态保持开关一致）；样式 components.css 追加 T-408 节（mat-stat/drawer 系 + drawer-in/fade 动画/src-card 系）。验收：py_compile 4 文件通过 + `npm run build` 通过（CSS 24.12kB/JS 527.79kB）+ 重启真实服务浏览器全量走查——①素材页：15 条统计/徽标正确，撤销标记→统计即时「已提炼 14·待提炼 1」且 frontmatter 顶格 compiled 回写（文件实测）→标记回来复原；预览抽屉徽标/meta/正文完整；来源筛选正常；②采集页：五源卡片渲染、meeting「引擎就绪」徽标（er=true）、飞书缺凭据 src-error 提示；开关为无名称视觉隐藏 checkbox 不进 a11y 树无 ref 可物理点击，以 API 等价链路验证——POST settings 持久化 false/true 双向落 config.yaml `sources.clipboard.enabled` + POST stop/start → /api/status stopped↔running 即时变化（enabled 字段联动），toggleSource 代码桥接受控 checkbox onChange 审查确认；③手动导入文本→「已导入：manual\manual_...md」素材计数 +1；④网页抓取→SPA 无正文页返回合理提示（建议手动导入）、静态页 example.com 完整入库 web\web_...md（走查产生的两条测试素材已清理） | `app/capture/inbox.py`、`app/capture/manager.py`、`app/capture/meeting_source.py`、`app/web/server.py`、`webui/src/pages/MaterialsPage.tsx`（重写占位）、`webui/src/pages/CapturePage.tsx`（重写占位）、`webui/src/styles/components.css`、`app/web/dist/`（构建产物）、`docs/m4-requirements.md`（§8 接口行补记单源启停）、`docs/tasks.md` | T-408 / M4 / REQ-408 |
| 2026-09-03 | **T-409 知识管理页完成（REQ-409/FR-041）**：①后端——`server.py` 新增 `PUT /api/knowledge`（`{path, content}` 写回，写前 `_resolve_entry` 防穿越）与 `DELETE /api/knowledge?path=`（删文件 + `update_index` 重建 index.md + `brain_mgr.remove(rel_path)`）；`_resolve_entry` 三路由（GET/PUT/DELETE）共用：resolve 后校验 `kd.resolve() not in target.parents` 或非 file 即 404，`..`/绝对路径越界不可达；`engine.py` 新增 `remove(doc_id)` 同步包装——按 `{cat}/{name}` 正斜杠 doc_id 找 `index_state.json` 中对应 full_doc_id，`adelete_by_doc_id`（LightRAG 清 doc_status/vdb_chunks 等）+ state 弹出 + `_save_state`，docstring 注明「索引批处理进行中删除可能残留，可由全量重建清理」。②前端——`KnowledgePage.tsx` EntryDetail 增编辑模式（「编辑」进入源码 textarea + 保存前确认弹窗「写回文件并提示重新索引」，取消可退）、删除按钮 + ConfirmModal（显示条目名与「同步清理大脑索引、不可撤销」后果）；保存成功经回调上移至父组件提示「已保存。大脑重新索引后问答/图谱生效」+「立即重新索引」按钮（POST `/api/brain/index`，完成后显示 scanned/inserted/skipped）；**bug 修复**：kbMsg/reindexing 状态上移父组件时 EntryDetail 内遗留 savedMsg 渲染块致 tsc TS2304 编译失败，删残留块后构建通过。验收：`npm run build` 通过（CSS 25.13kB/JS 530.85kB）+ 真实服务栈浏览器走查——①编辑流：v3 内容保存→确认弹窗→文件实测更新→详情页显示新内容→「立即重新索引」增量完成且 brain search 首条命中新内容；②删除流（静默管线两轮验证）：UI 删除→确认弹窗→列表即时移除→磁盘文件删→index.md 同步→LightRAG 六项全清（doc_status 无该 doc、vdb_chunks 0 行、index_state 无记录、search 无命中），第一轮曾遇批索引（28 文件）进行中并发删除致 LightRAG 残留（doc_status 保留 processed、chunks 残留）——定位为 ainsert 与 adelete 并发竞态而非 remove 代码缺陷，等管线静默后重跑删除流六项全清，caveat 已入 engine.remove docstring；③防穿越：PUT `../evil.md`、PUT 绝对路径、DELETE `../lessons/evil.md`、GET `../../secret.md` 全 404 且磁盘无残留；④残留 savedMsg 修复后编辑流重走查通过 | `app/web/server.py`、`app/brain/engine.py`、`webui/src/pages/KnowledgePage.tsx`、`webui/src/styles/components.css`、`app/web/dist/`（构建产物）、`docs/tasks.md` | T-409 / M4 / REQ-409 |
| 2026-09-03 | **T-410 设置页完成（REQ-410/FR-042）**：①后端——`server.py` `SettingsReq` 追加六个可选字段（`brain_enabled`/`brain_embedding_base_url`/`brain_embedding_model`/`brain_llm_base_url`/`brain_llm_model`/`llm_api_key`，None=跳过不写入）；新增 `_check_url` 助手（空串放行，否则必须 http(s):// 开头否则 422）；POST **校验先行**（端口 1-65535 + 三 URL 字段，任一非法 422 不做部分写入），brain 五项 `config.set(dotted)` 持久化 config.yaml，`llm_api_key` 走 `config.set_env("LLM_API_KEY")`（.env 行级替换/追加 + 同步 os.environ）敏感项不落 yaml；GET 追加 `brain` 节（enabled/embedding_base_url/embedding_model/llm_base_url/llm_model）+ 顶层与 compile 节各一份 `llm_key_configured: bool`，**恒不回读 Key 明文**。②前端——`SettingsPage.tsx` 全量重写六分组 panel（grid2 布局）：LLM API（compile.llm base_url/model + Key 密码框，placeholder 依 keyConfigured 显示「已配置（输入新值替换，留空保持不变）/未配置」）、Embedding（brain.embedding 两字段 + 重启重建索引提示）、大脑（enabled switch + brain.llm 两字段 + 留空回退编译 LLM 说明）、编译（两 switch + 开关即时生效提示）、监听与采集（file_dirs textarea 每行一个 + drop_dir 只读 kb-path + 五源 set-row switch + 飞书凭据密码框×2 + configured chip）、Web（端口 number + auto_start switch）；`validate()` 前端预检（端口 1-65535 整数 + 三 URL regex `^https?:\/\//`）；`save()` 敏感项留空不发（`if (llmKey.trim()) body.llm_api_key=…`），成功后 `applySettings` 回显 + 清空 Key 输入 + msg-ok「设置已保存。Web 端口、编译/大脑 LLM、Embedding 等项重启服务后生效。」；三态分支（loadErr 错误页/加载中/完整表单）；样式 components.css 追加 `.set-row`（行式开关布局）。验收：py_compile 通过 + `npm run build` 通过（CSS 25.35kB/JS 540.53kB）+ **API 层验证**——GET brain 节正确返回（enabled=true、embedding_base_url=ark v3、其余空）且 `llm_key_configured=true` 响应无明文；POST 非法值双向 422（llm_base_url=`ftp://bad`、web_port=99999）；落盘闭环（brain_llm_model=tmp-test-model → config.yaml 出现 → 恢复 ''）；Key 生命周期（写 dummy → .env 更新 → GET 无明文 → llm_key_configured=True → 原 48 字符 Key 恢复）+ **浏览器走查**——六分组渲染与表单回显正确（base_url/model/端口 8765/9 个 switch 状态全对/Key placeholder「已配置」/飞书「未配置」chip）；前端校验拦截两例 msg-err（端口 99999→「Web 端口需为 1-65535 的整数」、ftp://bad→「LLM API base_url 需为 http(s):// 开头的 URL 或留空」）；保存流 msg-ok + config.yaml 实测落盘 + UI 回显；保存后 Key 输入框清空、GET 响应无 `sk-` 明文；console 无当前 bundle 错误（历史累积日志均属旧构建与服务重启窗口期） | `app/web/server.py`、`webui/src/pages/SettingsPage.tsx`（重写占位）、`webui/src/styles/components.css`、`app/web/dist/`（构建产物）、`docs/tasks.md` | T-410 / M4 / REQ-410 |
| 2026-09-03 | **T-411 端到端验收与旧前端下线完成（REQ-411）——M4 关闭**：①端到端闭环走查（真实服务栈 + 真实数据 30 doc）——/ask 提问「剪贴板采集有哪些需要注意的问题？」→ busy 态正确（输入/模式/按钮 disabled + 「思考中」提示）→ ~30s LLM 回答完整 Markdown 渲染（七个 h3 小节 + References 列表 + inline code）→ 8 张引用卡片 URL 全部为完整相对路径（`/knowledge?path=queries%2F…` 等）→ 点击引用 [3] 跳转 `/knowledge?path=lessons%2Fwin32-clipboard-handle-truncation.md` 条目详情完整（frontmatter title/type/date/标签×4/关联站内链接/来源×2 素材链接 + 正文 h1/h2/list/code）→ /graph 图谱渲染 337 节点 · 544 关系（T-409 后增量索引扩容），工具栏/搜索/着色/缩放齐全；②能力覆盖不回退核对：采集开关（T-408 持久化+启停 API 链路）、素材标记与预览（T-408）、知识编辑/删除带确认（T-409 六项全清）、设置持久化（T-410 config.yaml/.env 落盘）+ 本次服务重启后设置页回显正常、编译链路（T-404）与大脑面板/侧边栏状态条正常；③**旧 static 下线**：`server.py` 删 `STATIC_DIR` 定义、`/static` mount 与 dist 缺失时的 STATIC_DIR index.html 兜底分支（dist 托管 + SPA fallback 保留），删除 `app/web/static/`（index.html + app.js）；重启服务验证：`/` + 七页路由 + `/api/status` 全 200（SPA fallback），旧路径 `/static/app.js` 落 SPA fallback 由前端 catch-all 呈现「404 页面不存在」（旧代码不可达、无死链白屏），首页/设置页渲染正常，服务日志无错误；④文档同步：tasks.md（T-411 done + T-011 doing→done + T-115 todo→done + 「下一步」M4 关闭）、milestones.md（M4 未开始→已完成 2026-09-03 + 验收留痕）、本文件 | `app/web/server.py`、`app/web/static/`（已删除）、`docs/tasks.md`、`docs/milestones.md` | T-411 / M4 / REQ-411 |
